//마이페이지 ~ 로그인 된 상태에서만 접근가능

import "./MyPage.css";
import { Link, useNavigate} from "react-router-dom";

const MyPage = () => {
    return(
        <div>
            마이페이지입니다
        </div>
    );
};

export default MyPage;
